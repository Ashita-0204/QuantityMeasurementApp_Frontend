services:
  - type: web
    name: buildscale-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/buildscale-angular/browser
    envVars:
      - key: API_URL
        value: https://quantitymeasurementappbackend.onrender.com
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html